# Assam Election ML — Production Requirements
# pip install -r requirements.txt

# ── Core (required) ───────────────────────────────────────────────────────────
flask>=3.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
requests>=2.28.0
python-dotenv>=1.0.0

# ── Google Trends (strongly recommended) ─────────────────────────────────────
pytrends>=4.9.0

# ── Optional: enhanced Twitter scraping ──────────────────────────────────────
# snscrape>=0.7.0          # real tweets — graceful Nitter RSS fallback if absent

# ── Optional: enhanced ML ─────────────────────────────────────────────────────
# xgboost>=2.0.0            # not required; sklearn GBM is default
# lightgbm>=4.0.0           # alternative ensemble member (future)
